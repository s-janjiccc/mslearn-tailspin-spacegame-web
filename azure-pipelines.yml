# C44EsfLCim6gAfkHZEND0NDQPqJUGs9sXFIXcKPTpCTatlzqFlLaJQQJ99BDACAAAAAAAAAAAAASAZDOVHrS

trigger:
- '*'
#- main

pool:
  name: 'Default'
    #vmImage: ubuntu-latest
    
variables:
    buildConfiguration: 'Release'

steps:
    - task: UseDotNet@2
      displayName: 'Use .NET SDK 6.x'
      inputs:
        packageType: sdk
        version: '6.x'

    - script: |
        export NVM_DIR="$HOME/.nvm"
        [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
        nvm install 16
        nvm use 16
        export PATH="$NVM_DIR/versions/node/v16.*/bin:$PATH"
        node -v
        npm -v
        rm -rf node_modules package-lock.json
        npm cache clean --force
        export PYTHON=/opt/homebrew/bin/python3.11
        npm install
      displayName: 'Use Node.js 16, verify version, try to install npm packages, compile sass assets'

    - script: |
        ls
        ls -la ./node_modules/.bin
      displayName: 'List current and node_modules/.bin directory'
      
    # - script: |
    #     pwd
    #     './node_modules/.bin/node-sass Tailspin.SpaceGame.Web/wwwroot --output Tailspin.SpaceGame.Web/wwwroot'
    #   displayName: 'show current dir & Compile SASS assets'

    - script: 'echo "$(Build.DefinitionName), $(Build.BuildId), $(Build.BuildNumber)" > buildinfo.txt'
      displayName: 'Write build info into file'
      workingDirectory: Tailspin.SpaceGame.Web/wwwroot

    - task: DotNetCoreCLI@2
      displayName: 'Restore .NET dependencies'
      inputs:
        command: 'restore'
        projects: '**/*.csproj'

    - task: DotNetCoreCLI@2
      displayName: 'Build .NET project'
      inputs:
        command: 'build'
        projects: '**/*.csproj'
        arguments: '--no-restore --configuration $(buildConfiguration)' # buildConfiguration is defined above in variables
